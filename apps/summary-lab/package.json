{
  "name": "lingxi-summary-lab",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "predev": "cargo build --release --manifest-path ../../Cargo.toml -p lingxi-cli",
    "dev": "node server.mjs --port 4174",
    "prestart": "cargo build --release --manifest-path ../../Cargo.toml -p lingxi-cli",
    "start": "node server.mjs --port 4174",
    "test": "node --test tests/*.test.mjs"
  }
}
